# RealSense RGB 数据采集：新人详细笔记

这份笔记解释仓库中每个程序在做什么、为什么这样做、会产生什么文件，以及数据怎样交给标注、训练、硬件和控制同学。即使你第一次接触相机、OpenCV 和数据集，也可以按顺序阅读。

## 1. 先建立整体认识

本项目最核心的动作是：从 RealSense 接收一帧彩色图，在窗口预览，等人按键，然后把当时的干净彩色帧写到指定目录。

~~~text
现实场景
  ↓ 镜头和传感器
RealSense 彩色流
  ↓ pyrealsense2
NumPy / OpenCV 的 BGR 图像
  ├─ copy 后画操作文字 → 屏幕预览
  └─ 不画文字的原始帧 → 按 r / f / 空格后写盘
~~~

turn1.py 和 turn2.py 会额外启用深度流：

~~~text
彩色帧 + 深度帧
  ↓ 把深度对齐到彩色图
用户点击彩色像素 (u, v)
  ↓ 读取该像素深度
用相机内参反投影
  ↓
相机光学坐标系中的三维点 [X, Y, Z]，单位米
  ↓ 历史实验旋转矩阵
“仅旋转后的坐标”
~~~

这仍不是完整视觉系统。仓库不做自动识别、标注、模型训练、目标跟踪、机器人外参标定、ROS 发布或 CAN 通信。

## 2. 输入、输出和不负责的部分

### 2.1 硬件输入

普通采集需要一台支持彩色流的 Intel RealSense。默认 profile 是 640×480、BGR8、30 FPS。collect_only.py 为低带宽场景默认使用 15 FPS。turn1.py 和 turn2.py 还请求同尺寸、同帧率的 Z16 深度流。

代码没有限定 D435i。相机是否支持请求的 profile，要以实际型号、固件和 SDK 查询结果为准。连接多台设备时应传 --device-serial，避免 SDK 选择了错误的相机。

### 2.2 人机输入

| 输入位置 | 输入 | 作用 |
| --- | --- | --- |
| OpenCV 窗口 | r | 保存到 real |
| OpenCV 窗口 | f | 保存到 fake |
| OpenCV 窗口 | q | 退出多数脚本 |
| OpenCV 窗口 | 空格 | collect_only.py 拍照 |
| OpenCV 窗口 | Esc | collect_only.py 退出 |
| Color Stream | 鼠标左键 | turn1.py / turn2.py 查询三维点 |
| OpenCV 窗口 | a | 转到终端重输角度 |
| 终端 | RX、RY、RZ | 手动旋转角，单位度 |
| 命令行 | --output-dir 等 | 设定路径、相机和流参数 |

cv2.waitKey 读取的是图像窗口按键。若 r/f/q 无反应，先点击窗口、切换英文输入法、关闭 Caps Lock。角度由终端 input 读取；按 a 后画面暂停并不一定是死机，它可能正在等终端输入。

### 2.3 文件与终端输出

双类脚本默认创建：

~~~text
captures/
├── real/
│   └── real_日期_时间_微秒_随机后缀.png
└── fake/
    └── fake_日期_时间_微秒_随机后缀.png
~~~

collect_images.py 用 JPG；collect_new.py、collect_simple.py、turn.py、turn1.py 和 turn2.py 用 PNG。collect_only.py 默认把 JPG 直接写入 captures/real，也可选择 PNG。

路径相对你执行命令时的当前工作目录解析，不依赖开发者电脑用户名。正式采集建议总是显式提供 --output-dir，并把输出放在 Git 仓库之外。

深度脚本只在终端输出坐标和距离。它们不会保存：

- 深度原图、点云或 RealSense bag；
- 点击坐标的 CSV/JSON；
- 相机内参、设备序列号或曝光元数据；
- 机器人基座坐标、TF、ROS topic 或 CAN 报文。

如果下游需要这些内容，必须先设计新的同步格式和数据契约，不能从 RGB 文件事后恢复。

## 3. 一次保存具体经历了什么

以 collect_new.py 为例：

1. argparse 解析输出目录、设备序列号、宽高、FPS 和超时。
2. prepare_class_directories 创建 real 和 fake 目录。
3. rs.context 查询是否有 RealSense。
4. rs.config 声明所需彩色流；若提供序列号则选择对应设备。
5. pipeline.start 启动相机。相机通常会被当前进程独占。
6. wait_for_frames 等待新帧，get_color_frame 取彩色图。
7. np.asanyarray 将 SDK 帧视图变成 OpenCV 可以处理的数组。
8. raw_image 是待保存帧；preview = raw_image.copy() 是显示副本。
9. putText 只修改 preview，imshow 显示它。
10. waitKey 得到用户按键。
11. r 或 f 触发 save_image_checked。它生成微秒时间戳和随机后缀，先确认文件不存在，再调用 imwrite。
12. imwrite 返回 False 时抛出明确错误；只有成功才打印保存路径。
13. q 或异常离开循环后，finally 只在成功启动过时 stop，并关闭窗口。

为什么必须区分 raw_image 和 preview？因为 OpenCV 绘图函数会原地修改数组。若直接在 raw_image 上写提示再保存，模型可能把固定位置的文字当作类别特征，数据也不再是传感器原始画面。

为什么时间戳后还要随机后缀？微秒已经很难碰撞，但系统时钟可能回拨、多个进程可能并行运行、数据可能被合并。短随机后缀和存在性检查提供额外保护。它不代表样本 ID；正式数据仍需要 manifest。

## 4. 七个脚本的区别

### 4.1 collect_new.py：默认入口

适合普通双类采集。它先检查设备、提供帧超时、保存 PNG，并安全释放相机。新人应先用它做冒烟测试。

局限：

- real/fake 的含义仍由团队定义；
- 不保存场次元数据和深度；
- 未自动做模糊、曝光、重复和隐私检查；
- PNG 文件通常比 JPG 大。

### 4.2 collect_images.py：重试与计数

它在每个 wait_for_frames 超时后最多重试若干次，并在预览中显示已成功保存的 real/fake 数量。历史版本的假样本打印使用未定义的 ffake_count，按 f 后会崩溃；公开副本已修复，并且计数只在写盘成功后增加。

可用参数：

~~~bash
python collect_images.py +  --output-dir ../private-captures/session-004 +  --timeout-ms 3000 +  --max-retries 5
~~~

重试不是无限的。连续失败通常说明 USB、供电、权限、设备占用或 profile 有问题，应停止排查，而不是无限循环制造假正常。

### 4.3 collect_only.py：低带宽单类

它只启用彩色流，默认 15 FPS，按空格保存到一个目录。适合只采目标类或 USB 链路不稳定的场景。

历史版本把路径写死为另一台电脑的 home 目录，并在启用自动曝光后又设置固定曝光。公开副本将路径改成 --output-dir，并把曝光分成互斥模式：

- 默认：打开自动曝光，不再设置固定 exposure。
- 提供 --manual-exposure VALUE：先关闭自动曝光，再请求固定值。

相机不保证支持任意数值。遇到 “option is read-only” 或 “value out of range”，先移除该参数，再用 RealSense Viewer/SDK 查询设备范围。

### 4.4 collect_simple.py：最短的教学版

它保留最直接的 pipeline → frame → preview → key → save 逻辑。历史版本在 start 失败后仍无条件 stop，可能用第二个异常掩盖真正原因；公开副本使用 started 标志。

正式采集通常使用 collect_new.py，因为它的错误信息和超时控制更清楚。

### 4.5 turn.py：名字容易误导的 RGB 版本

它不启用 IMU、深度或旋转计算，只是历史演变过程中的 RGB 采集器。保留它是为了让读者理解 turn1/turn2 的来源，不应因为文件名就认为它会输出转角。

### 4.6 turn1.py：点击三维点

流程：

1. 同时请求彩色与深度流。
2. rs.align(rs.stream.color) 把深度重采样到彩色坐标域。
3. 用户点击彩色像素。
4. depth_frame.get_distance 得到米制深度；这里只接受 0.1～5.0 m。
5. 使用 rs2_deproject_pixel_to_point 和对齐后的彩色内参反投影。
6. 用历史旋转矩阵得到 rotation-only frame 的坐标。
7. 打印一次结果并清空点击事件，避免历史版本逐帧刷屏。

对齐非常关键。未对齐时，同一个 (u,v) 在彩色和深度传感器中通常不是同一条视线。

### 4.7 turn2.py：增加距离

它在 turn1.py 基础上计算：

- 三维量程 r = sqrt(X² + Y² + Z²)，即相机光心到点的欧氏距离。
- 光轴径向偏移 rho = sqrt(X² + Y²)，即这个点相对 Z 光轴在成像横截面上的偏移。

历史注释把 rho 称为“水平距离”，容易被理解成地面 XY 平面的距离。公开副本改用“光轴径向偏移”。这两个量都在相机坐标约定下；相机倾斜后，不能把它们直接解释成机器人世界中的水平/高度。

## 5. 坐标和旋转为何只是实验

RealSense 光学坐标通常可理解为 X 向右、Y 向下、Z 向前，单位米，但集成时应以 SDK profile 和 ROS frame 约定为准。反投影从像素、深度和内参得到相机帧三维点。

脚本接着做：

~~~text
p_rotated = R^T p_camera
~~~

这里存在必须由团队确认的内容：

- RX/RY/RZ 是主动旋转还是坐标系被动旋转；
- 旋转顺序以及内旋/外旋；
- 正方向和角度测量来源；
- R 还是 R 转置才符合约定；
- 相机原点到世界/机器人基座原点的平移 t；
- 相机安装外参何时、如何标定；
- 相机帧时间如何与机器人状态同步。

完整刚体变换至少形如 p_base = R p_camera + t。当前代码没有 t，也没有 TF 树或标定文件，所以只称 rotation-only frame，绝不能把打印结果直接当作 base_link 世界坐标发送给控制器。

## 6. 正确的数据采集流程

### 6.1 采集前：先写数据契约

算法组至少回答：

1. 模型任务是分类、检测、语义分割还是实例分割？
2. 目标到底是整个物体、某个面、颜色区域还是关键点？
3. real/fake 的精确定义是什么？没有目标、遮挡、相似物和错误目标怎样处理？
4. 一张图可有几个目标？截断、反光和模糊达到什么程度仍有效？
5. 哪些距离、视角、光照、背景、物体实例和运动状态必须覆盖？
6. 是否需要同步深度、内参、外参、曝光和机器人姿态？
7. 如何按 session/场景/物体划分 train、validation、test？
8. 验收指标是分类准确率、mask IoU、中心点误差、距离误差、法向角误差，还是最终任务成功率？

没有书面答案，“多拍一些”通常只会产生更多相邻重复帧和含义不明的目录。

### 6.2 用 session 组织

不要把所有日期都写到一个长期累积目录。建议：

~~~text
private-captures/
└── 20260812_lab-a_session-001/
    ├── real/
    ├── fake/
    └── session-metadata.yaml
~~~

元数据建议记录：

- session_id、日期和高层地点代号；
- 相机型号、序列号、分辨率、FPS、固件/SDK；
- 自动/固定曝光和其他关键 sensor option；
- 物体 ID、目标面/姿态、距离区间；
- 光照、背景、遮挡、运动、操作者；
- 类别规范版本和本批用途；
- 来源、同意/授权、许可和 privacy_review 状态；
- 异常、失败写盘数和备注。

地点字段应使用不暴露敏感精确位置的项目代号；公开数据卡再决定披露粒度。

### 6.3 先少量测试

运行：

~~~bash
python collect_new.py --output-dir ../private-captures/SESSION_ID
~~~

各拍 3～5 张，退出，然后直接打开磁盘文件。检查：

- 图片能解码，尺寸与预期一致；
- 没有文字、红点或距离覆盖；
- 没有过曝、欠曝、严重模糊、黑帧；
- real/fake 路径没有按反；
- 连续按键不会覆盖旧文件；
- 背景没有人脸、屏幕、二维码或敏感设施。

### 6.4 按拍摄矩阵采集

一次有计划地改变变量。例如固定一个物体，依次覆盖近/中/远距离和正视/斜视/侧视，再换光照与背景。记录每个组合实际拍摄数。困难负样本要由算法规则定义，不能只凭操作者感觉。

### 6.5 退出、质检、清单

使用脚本退出键，看到进程结束后再拔设备。然后：

~~~bash
python tools/build_manifest.py +  ../private-captures/SESSION_ID +  --output ../private-captures/SESSION_ID-manifest.csv
~~~

抽查首、中、尾和随机样本；核对文件数量、清单行数和预期拍摄矩阵。清单工具只做基础文件级记录，不验证 class_hint。

### 6.6 去重、标注与切分

连续相机帧高度相似。不能随机把单张图片打散，否则同一拍摄序列会同时进入 train 和 validation/test，指标虚高。

正确方法：

- 先按 SHA-256 删除或关联完全相同副本；
- 再以感知哈希/图像嵌入检查相邻帧、重压缩、resize 和 crop 后的近重复；
- 把同一 session、物体实例、背景、源图和生成谱系作为 group；
- 按 group 分配 split；
- 先锁定独立测试 session，再做训练与调参；
- 标注导出后保留原图到标签的来源关系。

标注工具输出不是天然正确。标注组要提供类别表、polygon/box/keypoint 格式、遮挡/截断规则、空负样本规则和抽检结果。

## 7. 数据与代码为什么要分开发布

Git 擅长文本差异，不适合频繁变更的大量图像。把采集结果塞进源码仓会让每次克隆携带历史二进制，删除当前文件也不会清除过去提交中的内容。

更严重的是权利边界不同：

- 代码作者可以许可代码，却未必能许可镜头中的人、场地、屏幕和二维码；
- 一个第三方子集的许可证不能覆盖其他现场采集；
- 生成图片也要记录生成服务、条款和谱系；
- EXIF 为空不等于像素内容不敏感。

因此公开源码仓只放代码、文档、schema、数据卡、校验和与数据版本链接。原始数据先放私有/受控存储，完成 DATASET_POLICY.md 的审查后再发布到适合的数据平台。

推荐分工：

- Hugging Face Datasets：主要 ML 数据版本；审查中使用 private/gated。
- Zenodo：稳定冻结版和 DOI 归档。
- Kaggle：形成明确 benchmark 后的可选社区镜像。
- GitHub Release：少量经审查示例或便利包，不作为权威数据源。

## 8. 跨组合作契约

### 8.1 采集组交给标注/算法组

至少交付：

- 图片所在的不可变数据版本；
- session 元数据和 SHA-256 manifest；
- 类别/任务规范版本；
- 拍摄矩阵完成情况；
- 隐私、许可和来源审核状态；
- 无效/可疑/生成样本清单；
- 重复与近重复处理说明；
- 计划用途：训练、调参、验证或锁定测试。

不要仅发送一个名叫 train 或 new 的文件夹。

### 8.2 算法组反馈

算法组应返回：

- 使用的数据版本与 split manifest；
- 训练配置、基础模型、随机种子和依赖；
- 整体和分场景指标；
- 失败样例与原因分类；
- 下一轮补拍的可执行条件组合。

这样才能形成“采集—标注—训练—现场测试—失败补采”的闭环。

### 8.3 硬件与机械组

应提供并维护：

- 相机实际型号、序列号和固定方式；
- 相机到机器人基座/末端的标定方法和结果；
- 改支架、焦距、相机位置后是否必须重标定；
- 目标真实尺寸、工作距离和允许误差；
- USB 带宽、供电和部署主机约束。

### 8.4 ROS / 控制组

在任何三维结果接入控制前，共同确认：

- frame_id 和轴方向；
- 米或毫米；
- 采样时间戳、同步和最大允许延迟；
- 输出是面中心、法向量、特定目标点还是其他量；
- 多目标选择逻辑；
- 置信度、有效标志、丢目标、超时和异常值行为；
- 发布频率、滤波和安全边界。

当前程序不发布 ROS、TF、CAN 或串口数据。终端打印不是接口契约。

## 9. 常见故障

### 9.1 ModuleNotFoundError

先确认 IDE 和终端使用同一个 Python：

~~~bash
which python
python -m pip show pyrealsense2 opencv-python numpy
python -c "import cv2, numpy, pyrealsense2; print('OK')"
~~~

在 Windows 用 where python。在 ARM/Jetson 上可能需要按平台编译/安装 librealsense Python 绑定。

### 9.2 No device connected

按顺序检查：

1. rs-enumerate-devices 是否看到相机；
2. USB 线是否支持数据和 USB 3.x；
3. 是否被 Viewer、ROS 或另一个 Python 进程占用；
4. 当前用户的 udev/设备权限；
5. 容器是否映射 USB 与显示设备；
6. 多相机时 --device-serial 是否正确。

### 9.3 Couldn't resolve requests

请求的分辨率、格式、FPS 组合不受设备支持，或彩色/深度组合超过带宽。先使用默认 640×480@30；深度不需要时换普通 RGB 脚本；仍失败时用 SDK 工具列出 profile。

### 9.4 Frame didn't arrive

- 换主板直连 USB 3.x 端口和可靠线缆；
- 关闭 HUB 上的高带宽设备；
- 关闭其他相机进程；
- 尝试 collect_only.py 的 15 FPS；
- 检查供电、温度、固件和 SDK 日志。

collect_images.py 的重试能处理偶发超时，不能修复物理链路。

### 9.5 窗口打不开或按键无反应

这些脚本要求 OpenCV GUI。SSH、容器和无桌面服务器需要正确图形转发；opencv-python-headless 不支持 imshow。按键时焦点要在图像窗口。

### 9.6 打印 Save failed

检查输出目录权限、磁盘空间、文件系统是否只读，以及路径所在设备是否断开。公开副本不会在写盘失败时增加计数，也不会谎报成功。

### 9.7 图片里仍有文字

先确认查看的是公开副本新采集的文件，而不是历史数据。所有当前脚本都保存 raw_image，只在 preview 上绘图。若二次修改代码，搜索 putText/circle 与 imwrite，确认两者操作的不是同一个数组。

### 9.8 点击无深度

反光、透明、纯黑材质、过近、过远和遮挡边缘都可能产生无效深度。本实验过滤了小于 0.1 m 或大于 5.0 m 的值。查看 Depth Stream，并在物体内部而非边界点击。

### 9.9 旋转坐标不符合机器人

这是预期风险：程序缺少平移、标定、旋转约定验证和机器人 TF。不要通过“调角度看起来差不多”代替手眼/外参标定。

## 10. 公共代码仍有哪些边界

这次清理解决了明显的公开质量问题：

- 修复假样本计数拼写错误；
- 移除开发机绝对路径；
- 输出目录可配置；
- 预览覆盖与保存原帧分离；
- 文件名具备碰撞保护；
- 检查每次 imwrite；
- 相机只在成功启动后 stop；
- 手动曝光与自动曝光互斥；
- 点击事件处理后清空；
- 使用 SDK 标准反投影函数；
- 明确旋转坐标不是世界/机器人坐标。

但它仍是轻量、人工交互的采集工具，不包括：

- 自动 session metadata；
- 同步 RGB-D 录制；
- 完整标定与 ROS 集成；
- 自动模糊/曝光/隐私审核；
- 近重复检测与数据切分；
- 自动标注、训练或评估；
- 可复现实机集成测试。

若要增加这些功能，先写需求和数据格式，再添加新入口；不要让七个历史脚本继续无边界分叉。

## 11. 公开前最后检查

维护者应逐项确认：

- [ ] 权利人已选择并添加 LICENSE。
- [ ] 法律/项目审批已完成。
- [ ] git status 中没有图片、标签、压缩包、模型或录包。
- [ ] git 历史从未包含上述敏感大文件。
- [ ] rg 不再命中开发者 home 路径、凭证、IP、邮箱或设备序列号。
- [ ] requirements 在目标 Python/系统上实际安装验证。
- [ ] python -m py_compile *.py tools/*.py 通过。
- [ ] 所有脚本的 --help 通过。
- [ ] 有真实 RealSense 的人完成 RGB 冒烟采集。
- [ ] 有深度设备的人验证对齐、点击和单位。
- [ ] 仓库描述明确“不包含数据集”。
- [ ] 若链接外部数据集，其数据卡、许可、隐私和切分审核均完成。

## 12. 仍需项目负责人决定

1. real/fake 的正式业务定义是否保留，还是改成 task-specific 类名？
2. 七个脚本是否全部作为历史教学版本保留，还是以后只维护 collect_new.py 和 turn2.py？
3. 正式支持哪些 RealSense 型号、操作系统和 Python 版本？
4. 代码采用哪一种许可证？
5. 是否需要保存同步深度、内参、外参和机器人姿态？
6. 完整坐标系、旋转顺序和标定方法是什么？
7. 数据集是否有可公开子集；每个子集的权利人和许可证是谁？
8. 数据最终托管地址和版本命名规则是什么？

在这些问题得到确认前，可以使用代码做私有冒烟测试，但不要把未经审查的数据加入 Git 或公开数据平台。
